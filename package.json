{
  "name": "familiara-root",
  "version": "0.1.0",
  "private": true,
  "description": "Root package — provides shared dependencies for the monorepo",
  "dependencies": {
    "firebase": "^10.12.4"
  }
}
